# このプロジェクトで作業するときのルール

## 🔴 最重要：作業を始める前に必ずGitHubと同期する

このリポジトリのオーナーは**非エンジニア**で、複数のPCを使い分けている。
本人は同期(git pull)の操作を覚えていないし、毎回お願いするとは限らない。
**「このフォルダを見て」程度の一言＝「まず最新に同期してから作業して」という意味**として扱うこと。

### 具体的な手順（このフォルダで最初に話しかけられたら）

1. まだこのセッションで同期確認をしていなければ、次を実行して同期状態を確認・自動取り込みする：
   ```
   echo '{}' | python .claude/hooks/git_sync_check.py
   ```
   - SessionStartフックが既に実行済みなら、その結果を信頼してよい（このスクリプトは冪等なので、念のため再実行しても安全）。
   - スクリプトはJSONで結果を返す（最新だった / 自動取り込みした / 要対応）。
2. 結果をユーザーに**普通の日本語で一言だけ**伝えてから、本題に入る。
   例：「✅ GitHubの最新に同期したよ。では…」

### 同期で警告（⚠️）が出たとき

- **勝手に上書きしない。** `git reset --hard` や強制pullで一方を消すのは厳禁。
- 「ローカルに未保存の変更」or「分岐(divergence)」が原因。ユーザーに分かる言葉で状況を説明し、
  どちらの変更を残すか確認してから、commit/stash → pull の順で安全に整理する。

---

## このアプリについて

- **英語リスニング学習クイズ**（単語/フレーズ/慣用句 × 音声・意味・スペルの各モード）。
- ソースは `src/build_html.py`（HTMLを生成するビルドスクリプト）。**直接 `output/app/index.html` を編集しない** — 必ず `src/build_html.py` を直し、`python src/build_html.py` で再生成する。
- 生成物：`output/app/index.html`（ローカル確認用）と `docs/index.html`（GitHub Pages公開用）。両方が同時に更新される。
- 進捗・XP・実績などはブラウザの localStorage に保存（サーバーなし）。

## 公開フロー（GitHub）

- 変更は commit → `git push origin main` して初めてGitHubと公開サイト(GitHub Pages)に反映される。
- 公開して困る秘密情報は持たない設計（Dropbox連携はPKCE方式・トークンは各自のブラウザ内）。
